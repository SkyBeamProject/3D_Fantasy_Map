# Overview

Assets and rendering configuration for a 3D fantasy map. Physical terrain sources live under `Physical/`, web-facing viewer assets live under `Web/`, and gradient/color ramp definitions are tracked in `Gradient Details.txt`.
